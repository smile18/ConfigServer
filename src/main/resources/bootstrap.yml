spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/smile18/spring-config-repo
          #https://github.com/smile18/spring-config-repo
          #https://github.com/spring-cloud-samples/config-repo
        encrypt:
          enabled: false
server:
  port: 8888

